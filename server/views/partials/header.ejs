 <!DOCTYPE html>
<html>
  <head>
    <title><%= title %> page</title>

    <!-- Bootstrap (via CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Font Awesome (via CDN) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/content/app.css">
  </head>
  <body>
  

  <!--Nav Bar-->
   <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">INFR3120</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
          aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <% if(title === 'Home'){ %>
            <a class="nav-link active" aria-current= "page" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
            <% } else { %>
            <a class="nav-link" aria-current= "page" href="/"><i class="fa-solid fa-lg fa-house"></i> Home</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if(title === 'Workouts'){ %>
            <a class="nav-link active" aria-current= "page" href="/workouts"><i class="fa-solid fa-lg fa-dumbbell"></i> Workouts</a>
            <% } else { %>
            <a class="nav-link" aria-current= "page" href="/workouts"><i class="fa-solid fa-lg fa-dumbbell"></i> Workouts</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if(title === 'Workout Entries'){ %>
            <a class="nav-link active" aria-current= "page" href="/entries"><i class="fa-solid fa-book"></i> Entries</a>
            <% } else { %>
            <a class="nav-link" aria-current= "page" href="/entries"><i class="fa-solid fa-book"></i> Entries</a>
            <% } %>
          </li>
        </ul>
      </div>
    </nav>
